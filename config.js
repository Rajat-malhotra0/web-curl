const CONFIG = {
    // The URL for the CORS proxy server
    // Local: 'http://localhost:3001/proxy?url='
    // Production: 'https://cors-proxy-six-flame.vercel.app/proxy?url='
    PROXY_URL: 'https://cors-proxy-six-flame.vercel.app/proxy?url=',

    // The URL for the home/landing page
    // Use '#' if standalone, or '../index.html' if part of a suite
    HOME_URL: '#'
};
